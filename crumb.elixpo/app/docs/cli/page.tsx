import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata({ title: 'CLI and Native Shell', description: 'Use Crumb as a real Bash, Zsh, or PowerShell terminal and understand when natural-language intelligence is invoked.', path: '/docs/cli', keywords: ['Crumb CLI', 'native AI shell', 'natural language command line'] })
export default function CliPage() { return <article><p className="docs-eyebrow">CLI guide</p><h1>A real terminal first.</h1><p className="docs-lede">Crumb preserves your native shell experience. Normal command lines are not rewritten, interpreted by a model, or sent to a provider.</p><h2 id="native-commands">Native commands</h2><p>Programs, pipes, redirects, shell expansions, and full-screen terminal applications belong to your native shell. Persistent state includes the current directory and exported environment variables.</p><pre><code>{'git status\ncargo test -p crumb-cli\nfind . -name \\'*.rs\\' | wc -l\ntop'}</code></pre><h2 id="crumb-built-ins">Crumb built-ins</h2><p>Crumb-specific controls are deliberately separate from native commands. Use the built-in help inside the current executable to see what is supported by that release.</p><pre><code>{':help\n:platform\n:version\n:exit'}</code></pre><h2 id="natural-language">Natural-language requests</h2><p>Natural language enters an isolated skill and agent path. A skill describes its activation rules, preferred model, allowed tools, and required approvals. The shell does not silently reinterpret a command as an AI prompt.</p><div className="docs-callout"><b>Availability</b><p>The complete natural-language harness is under development. Documentation will show the final invocation grammar when that work package lands.</p></div><h2 id="failure-behavior">Failure behavior</h2><p>Network failures, provider errors, and unavailable models must not block startup or native terminal work. Provider-specific code stays outside the shell core.</p></article> }
