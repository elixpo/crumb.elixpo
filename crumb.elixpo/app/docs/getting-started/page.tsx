import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata({ title: 'Getting Started', description: 'Install the Crumb Natural Language Terminal, launch its native shell, and prepare secure account authentication.', path: '/docs/getting-started', keywords: ['install Crumb terminal', 'Crumb quickstart', 'Rust AI terminal'] })
export default function GettingStartedPage() { return <article><p className="docs-eyebrow">Quickstart</p><h1>Run Crumb locally.</h1><p className="docs-lede">The project currently builds from source with the stable Rust toolchain. Crumb runs as the <code>crumb</code> executable.</p><h2 id="requirements">Requirements</h2><ul><li>Rust and Cargo through <a href="https://rustup.rs">rustup</a>.</li><li>Git for cloning and updating the repository.</li><li>Bash on Linux, Zsh on macOS, or modern PowerShell on Windows.</li></ul><h2 id="install-from-source">Install from source</h2><pre><code>{'git clone https://github.com/elixpo/crumb.elixpo.git\ncd crumb.elixpo\ncargo build --release -p crumb-cli\n./target/release/crumb'}</code></pre><p>During development, launch directly with:</p><pre><code>cargo run -p crumb-cli</code></pre><h2 id="use-the-native-shell">Use the native shell</h2><p>Enter commands exactly as you would in your regular terminal. Directory and environment changes persist because Crumb owns one interactive native shell session.</p><pre><code>{'pwd\ncd ~/projects\nexport CRUMB_DEMO=ready\necho \"$CRUMB_DEMO\"'}</code></pre><h2 id="editing-shortcuts">Edit longer requests</h2><ul><li><code>Alt+Enter</code> or <code>Shift+Enter</code> inserts a newline.</li><li><code>Ctrl+O</code> opens the current buffer in <code>$VISUAL</code> or <code>$EDITOR</code>.</li><li><code>Tab</code> opens width-safe command and context suggestions.</li><li><code>Ctrl+R</code> searches history.</li></ul><h2 id="accessible-output">Choose accessible output</h2><p>Use <code>NO_COLOR=1</code>, <code>CRUMB_REDUCED_MOTION=1</code>, <code>CRUMB_PLAIN=1</code>, or <code>CRUMB_SCREEN_READER=1</code>. Screen-reader mode also disables startup art and animation.</p><h2 id="prepare-ai-features">Prepare connected features</h2><p>Sign in to the Crumb website, connect Pollinations under Profiles, then authorize your terminal with <code>crumb auth login</code>. Native commands continue working before, during, and after this setup.</p></article> }
