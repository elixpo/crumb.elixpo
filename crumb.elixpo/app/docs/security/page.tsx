import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata({ title: 'Security Model', description: 'Understand Crumb security boundaries for native shell execution, agent permissions, secrets, and provider failures.', path: '/docs/security', keywords: ['AI terminal security', 'agent permission model', 'secure native shell'] })
export default function SecurityPage() { return <article><p className="docs-eyebrow">Architecture</p><h1>Explicit boundaries by design.</h1><p className="docs-lede">A useful AI terminal must fail safely. Crumb treats shell ownership, permissions, credentials, and provider isolation as architecture—not prompts.</p><h2 id="shell-ownership">Shell ownership</h2><p>Bash, Zsh, or PowerShell executes normal commands. Crumb does not duplicate or approximate native shell semantics.</p><h2 id="agent-isolation">Agent isolation</h2><p>Interactive shell state and agent execution environments are separate. Context may be passed deliberately, but an agent cannot silently become the user&apos;s shell.</p><h2 id="permissions">Permissions</h2><p>Models cannot grant themselves permissions. Skills describe capabilities, while the runtime and user approval enforce the actual boundary.</p><h2 id="secrets">Secrets</h2><p>Credentials are not written to logs or command history. Website sessions use HTTP-only cookies, provider connectors are encrypted, and CLI credentials use the operating-system keychain.</p><h2 id="failure-resilience">Failure resilience</h2><p>Network activity never blocks terminal startup. Native commands stay available when a model, provider, connector, or the entire intelligence layer fails.</p></article> }
