import Link from 'next/link'
import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata({ title: 'Authentication and Device Flow', description: 'Securely sign in to Crumb with Elixpo Accounts and authorize the CLI through a one-time browser device flow.', path: '/docs/authentication', keywords: ['Crumb device flow', 'terminal OAuth login', 'Elixpo Accounts authentication'] })
export default function AuthenticationPage() { return <article><p className="docs-eyebrow">Authentication</p><h1>Browser trust, terminal convenience.</h1><p className="docs-lede">Crumb separates your website session, provider authorization, and local terminal credential. Passwords and provider keys do not need to cross the command line.</p><h2 id="website-sign-in">Sign in to the website</h2><ol><li>Open the Crumb login screen and choose <strong>Continue with Elixpo</strong>.</li><li>Authenticate with Elixpo Accounts.</li><li>Accounts redirects to Crumb&apos;s verified callback and creates an HTTP-only session.</li></ol><h2 id="connect-a-provider">Connect a provider</h2><p>Open <Link href="/profile/connectors">Profiles</Link> after login. Provider authorization happens in the browser and returns to Crumb through a dedicated callback.</p><h2 id="authorize-the-cli">Authorize the CLI</h2><pre><code>crumb auth login</code></pre><ol><li>The CLI requests a one-time device code.</li><li>Open the displayed verification URL and approve that device.</li><li>The CLI polls for completion and stores the final scoped credential in the operating-system keychain.</li></ol><h2 id="requested-account-scope">Account scope</h2><p>The website sign-in requests <code>openid profile email</code>. Device registration uses the dedicated Crumb CLI client, so browser and terminal clients remain distinct.</p><h2 id="sign-out-and-revoke">Sign out and revoke</h2><p>Signing out deletes the current Crumb website session. Revoke a provider connection at its source when it must stop working everywhere.</p></article> }
