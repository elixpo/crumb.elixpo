import Link from 'next/link'
import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata({ title: 'Connect Pollinations', description: 'Connect Pollinations to Crumb safely from your Profiles page without pasting an API key into the terminal.', path: '/docs/pollinations', keywords: ['Crumb Pollinations connector', 'Pollinations OAuth', 'secure AI provider connection'] })
export default function PollinationsPage() { return <article><p className="docs-eyebrow">Provider connection</p><h1>Connect Pollinations from Profiles.</h1><p className="docs-lede">The Profiles route is the control center for services connected to Crumb. Pollinations is the first model provider supported by this browser handshake.</p><h2 id="before-you-start">Before you start</h2><p>You need an Elixpo account and a Pollinations account you control. Sign in to Crumb before opening the provider connection.</p><h2 id="connect-from-profiles">Connect from Profiles</h2><ol><li>Open <Link href="/profiles">Profiles and connections</Link>.</li><li>Find the Pollinations provider card and choose <strong>Connect Pollinations</strong>.</li><li>Review and approve the requested access on Pollinations.</li><li>Return to Profiles and confirm that the provider state reads <strong>Connected</strong>.</li></ol><p>If the authorization expires or changes, use <strong>Reconnect Pollinations</strong> from the same card.</p><h2 id="authorize-your-terminal">Authorize your terminal</h2><p>After the browser connection succeeds, run:</p><pre><code>crumb auth login</code></pre><p>Approve the one-time device request in your browser. The CLI then exchanges its grant through Crumb instead of asking for your Pollinations key.</p><h2 id="model-policy">Default model policy</h2><ul><li><strong>Text:</strong> Nova Fast and Qwen Coder are combined by the harness.</li><li><strong>Images:</strong> Flux and Klein are available through the image path.</li><li><strong>Other modalities:</strong> Crumb selects one explicit default model per modality.</li></ul><h2 id="security-boundary">Security boundary</h2><p>Connector credentials are encrypted at rest and provider details stay outside <code>crumb-core</code>. The terminal receives only the scoped authorization it needs.</p></article> }
