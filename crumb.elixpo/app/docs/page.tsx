import Link from 'next/link'
import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata({ title: 'Developer Documentation', description: 'Learn how Crumb combines a native shell, natural-language skills, secure device authentication, and connected model providers.', path: '/docs', keywords: ['Crumb documentation', 'natural language terminal docs', 'AI shell documentation'] })
export default function DocsOverview() { return <article><p className="docs-eyebrow">Crumb NLT documentation</p><h1>Build fluency at the command line.</h1><p className="docs-lede">Crumb is a Natural Language Terminal: native commands keep their real shell behavior, while explicit natural-language requests can use models, skills, and tools.</p><div className="docs-callout"><b>Current development status</b><p>The native shell, browser account connection, Pollinations connector, and device authorization foundations are available. The complete agent harness is still being integrated.</p></div><h2 id="choose-a-path">Choose a path</h2><div className="docs-card-grid"><Link href="/docs/getting-started"><b>Getting started</b><span>Requirements, installation, and your first Crumb session.</span></Link><Link href="/docs/cli"><b>CLI and native shell</b><span>Learn native commands, built-ins, history, and natural-language boundaries.</span></Link><Link href="/docs/authentication"><b>Secure authentication</b><span>Understand browser sign-in and the terminal device flow.</span></Link><Link href="/docs/pollinations"><b>Connect Pollinations</b><span>Bind your provider account safely from Profiles.</span></Link></div><h2 id="core-guarantees">Core guarantees</h2><ul><li>Ordinary commands never require AI or network access.</li><li>Bash, Zsh, and PowerShell retain native command semantics.</li><li>Provider credentials do not need to be pasted into terminal history.</li><li>Models cannot grant themselves permissions.</li><li>The terminal remains useful when every AI service is unavailable.</li></ul><h2 id="quick-reference">Quick reference</h2><pre><code>{'cargo run -p crumb-cli\ncrumb auth login\n# Then follow the one-time browser verification link.'}</code></pre></article> }
