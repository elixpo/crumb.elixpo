import type { Metadata } from 'next'
import { InfoPage } from '@/app/components/InfoPage'

export const metadata: Metadata = { title: 'Documentation', description: 'Get started with Crumb LNT and its secure provider device flow.' }
export default function DocsPage() { return <InfoPage eyebrow="Documentation" title="Start with your shell. Add intelligence." intro="Crumb is under active development. These are the current authentication and interaction paths."><h2>Run Crumb</h2><pre><code>cargo run -p crumb-cli</code></pre><p>Commands such as <code>pwd</code>, <code>cd</code>, and full-screen tools execute through the persistent native shell.</p><h2 id="authentication">Connect and authenticate</h2><ol><li>Sign in at the Crumb site with your Elixpo account.</li><li>Connect Pollinations from the Connections screen.</li><li>Run <code>crumb auth login</code> and approve the displayed device code.</li></ol><p>The browser holds the provider authorization flow. The terminal receives a scoped connector instead of asking you to paste a provider key.</p><h2>Natural language</h2><p>Plain-English requests are routed to an appropriate skill and model harness. Crumb uses Nova Fast and Qwen Coder for text work, while provider details stay outside the native shell core.</p></InfoPage> }
