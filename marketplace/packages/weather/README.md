# Crumb Weather MCP

This package provides weather lookup capabilities using the keyless Open-Meteo API.
When enabled and granted network access, it adds the `@plugin:weather` capability.

Command: `@plugin:weather <city>`
