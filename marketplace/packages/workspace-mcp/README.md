# Crumb workspace MCP

This package exposes Crumb's built-in MCP server through `crumb mcp serve`. Installation
only records its launch metadata. The server remains disabled until the user explicitly
enables it and approves its declared workspace-read and process-execution capabilities.
